const config = {
  API_BASE_URL: "https://globalindiabackendnew.onrender.com",
  // Add other configuration variables here if needed
  SOCKET_TIMEOUT: 5000,
  MAX_RETRY_ATTEMPTS: 3,
};

export default config;